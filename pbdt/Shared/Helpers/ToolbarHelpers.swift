//
//  ToolbarHelpers.swift
//  pbdt
//
//  Created by Andrew M Levy on 5/10/19.
//  Copyright © 2019 Andrew-M-Levy. All rights reserved.
//

import Foundation
import UIKit

extension UIToolbar {
    /*
    func setupToolbar(toolbar: UIToolbar, view: UIView) {
        
        toolbar.barStyle = UIBarStyle.default
        toolbar.isTranslucent = true
        toolbar.isUserInteractionEnabled = true
        toolbar.tintColor = UIColor.toolbarText()
        toolbar.barTintColor = UIColor.toolbarBackground()
        // buttons
        let doneButton = UIBarButtonItem(title: "Done", style: UIBarButtonItem.Style.done, target: self, action: #selector(doneClicked(view: view)))
        let flexibleSpace = UIBarButtonItem(barButtonSystemItem: .flexibleSpace, target: nil, action: nil)
        // setup
        toolbar.setItems([flexibleSpace, doneButton], animated: false)
    }
    
    // done toolbar button clicked
    @objc func doneClicked(view: UIView) {
        // end editing
        view.endEditing(true)
    }
    */
}
